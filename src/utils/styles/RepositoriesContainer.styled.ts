import styled from "styled-components";
import { Box } from "@mui/material";

export const RepositoriesContainer = styled(Box)`
    display: flex;
    flex-direction: row;
    align-items: start;
    flex-wrap: nowrap;
    gap: 8px;`;

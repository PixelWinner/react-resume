import styled from "styled-components";
import { Paper } from "@mui/material";

export const ResumeContainer = styled(Paper)`
    display: flex;
    flex-direction: column;
    margin: auto;
    padding: 16px;
    width: fit-content;
    gap: 16px;`;
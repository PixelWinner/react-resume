import styled from "styled-components";
import { Box } from "@mui/material";

export const LanguagesContainer = styled(Box)`
    display: flex;
    align-items: start;
    flex-direction: row;
    gap: 8px;
`;